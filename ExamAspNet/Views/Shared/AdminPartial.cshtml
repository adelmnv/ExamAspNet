@if (Session["id"] == null)
{
    <li>@Html.ActionLink("Log In", "LogIn", "Admin")</li>
}
else
{
    <li>@Html.ActionLink($"Hello {Session["name"]}", $"Details/{Session["id"]}", "Admin")</li>
    <li>@Html.ActionLink("Exit", "Index", "Gym")</li>
}
